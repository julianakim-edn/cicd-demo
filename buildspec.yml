version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies..."
      - npm install || true
  build:
    commands:
      - echo "Zipping source code..."
      - zip -r build.zip index.js
artifacts:
  files:
    - build.zip
